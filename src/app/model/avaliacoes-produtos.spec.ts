import { AvaliacoesProdutos } from './avaliacoes-produtos';

describe('AvaliacoesProdutos', () => {
  it('should create an instance', () => {
    expect(new AvaliacoesProdutos()).toBeTruthy();
  });
});
