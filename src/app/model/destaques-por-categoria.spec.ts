import { DestaquesPorCategoria } from './destaques-por-categoria';

describe('DestaquesPorCategoria', () => {
  it('should create an instance', () => {
    expect(new DestaquesPorCategoria()).toBeTruthy();
  });
});
