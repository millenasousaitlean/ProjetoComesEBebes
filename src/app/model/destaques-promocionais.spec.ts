import { DestaquesPromocionais } from './destaques-promocionais';

describe('DestaquesPromocionais', () => {
  it('should create an instance', () => {
    expect(new DestaquesPromocionais()).toBeTruthy();
  });
});
